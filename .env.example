# Copy values into process environment variables; the API does not auto-load .env.
GUOJING_DATABASE_URL=sqlite:///./data/guojing.db

# Generate a private value, for example: openssl rand -hex 32
GUOJING_ADMIN_API_TOKEN=replace-with-at-least-32-random-characters
