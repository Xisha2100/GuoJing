# 模块 51：MVP 场景模板

场景模板覆盖已确认的 13 个常用任务：微信添加好友、拉群、发语音、收红包、线下支付、余额；抖音搜索；网购选择；滴滴打车；高德导航；拨打电话、通讯录和图库。

模板使用通用语义锚点和 `PROVISIONAL` 状态，不能直接发布。管理员必须在真实设备上录制并替换锚点。红包、支付、余额、拨号等步骤带有 `FINANCIAL` 或 `SENSITIVE` 风险，执行引擎会停下并要求人工确认。
