# 模块 40：相机拍照教程样例

首个垂直样例是系统相机的“拍照”。`camera_capture_tutorial()` 返回一个待发布的教程图：相机预览节点以拍照按钮为锚点；低风险 transition 只说明“请你亲自点击”；拍摄完成节点以最近照片缩略图验证结果。

示例特意标记为 `LOCAL_ONLY`，因为相机画面可能包含高度私密内容。不同厂商的相机 resource ID 和无障碍描述不同，管理员必须在目标设备录制、核验后才能创建修订并发布；这个工厂函数不是把模板直接推送给用户。

这体现了状态图的价值：UI 改动后可替换锚点或重新验证某一节点，而不是重录一段固定坐标视频。
