# 模块 37：有界重试策略

`RetryPolicy` 只计算下一次可尝试的延迟，不 sleep、不访问队列。第 1、2、3 次失败按指数增长，超过 `max_delay` 后封顶；达到最大尝试次数返回 `None`。

将调度决策从 worker 拆成纯函数，可用单元测试验证边界，也避免模型超时后出现无穷重试。实际 worker 应把 `None` 转成明确的人审状态，而不是继续抢占租约。
